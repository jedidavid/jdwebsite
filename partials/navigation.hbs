<!-- Menu Navigation -->
{{!< default}}

<div class="primary-overlay">
    <div class="navbar-fixed navmenu">
        <nav class="white   ">
            <div class="nav-wrapper">
                <div class="container">
                    <div class="row">
                        <div class="col s12">
                            <a href="#!" class="brand-logo">
                                <img src="../../assets/images/logo1.png" alt="" class="img-responsive">
                            </a>
                            <a href="#" data-activates="mobile-nav" class="button-collapse">
                                <i class="fa fa-bars fa-2x" style="color:#43a047"></i>
                            </a>
                            <ul class="right hide-on-med-and-down" role="menu">
                                {{#foreach navigation}}
                                <li class="nav-{{slug}}{{#if current}} nav-current{{/if}}" role="menuitem">
                                    <a href="{{url absolute=" true "}}" class="waves-effect navlinks grey-text text-darken-2">{{label}}</a>
                                </li>
                                {{/foreach}}
                            </ul>
                        </div>
                    </div>
                </div>
                <ul class="side-nav" id="mobile-nav">
                    {{#foreach navigation}}
                    <li class="nav-{{slug}}{{#if current}} nav-current{{/if}}" role="menuitem">
                        <a href="{{url absolute=" true "}}" class="waves-effect blue-grey-text darken-4">{{label}}</a>
                    </li>
                    {{/foreach}}
                </ul>
            </div>
        </nav>
    </div>
</div>